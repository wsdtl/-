# 晓楠修仙核心设定

## 两条修行路

- 闭关炼体：恢复人物状态、增长人物修为，并从全功法池随机感悟功法。
- 山野探险：连续遭遇低阶灵兽，消耗体力，获得灵石、本命武器经验和战利品。

功法主要改变战斗方式，本命武器主要提供战斗能力。两条成长线在战斗中汇合，但资产和成长规则
彼此独立。

## 人物与功法

- 功法有品级和随机词条，没有等级；每门功法由固定属性、主动技能和被动技能等 JSON 原子能力组合而成。
- 固定属性按功法原值生效；品级威力倍率作用于主动与被动效果，随机词条保留出生时掷出的实际数值。
- 玩家最多同时装配六本功法，同名功法不能同时装配。
- 多本功法同时生效；可用主动技能按轮转顺序施展，被动技能按各自触发条件独立结算。
- 人物每五级进入待突破；突破前等级和经验停止增长，溢出经验丢弃。
- 当前小循环只保留待突破状态，突破情境和三选一奖励以后单独实现。

## 本命武器

- 人物出生时获得唯一一把“用户名的本命武器”，它不是纳戒物品，不能交易或替换。
- 本命武器通过探险获得经验并升级。
- 六个附魔位、六个宝石位和网页创造模式已经预留数据位置，当前小循环不提前实现操作流程。

## 时间活动

- 闭关和探险都是 60 分钟，每个完整 10 分钟算一轮。
- 提前结束只领取完整轮次，未满 10 分钟的部分没有轮次奖励，也不附加二次等待限制。
- 奖励只在主动结束时落入资产；完成 60 分钟但尚未结束的活动保持待结算。
- 探险连续继承血气、精神、体力、护盾、临时状态、技能冷却、主动招式轮转位置和丹药消耗。

## 世界与行路

- 当前只运行一个整数坐标世界，人物出生在青溪村 `(0, 0)`。
- 地点坐标、世界边界和当地功能均由 JSON 明确定义，人物可按地点名即时前往任意已登记地点。
- 行路距离采用曼哈顿距离 `|横轴差| + |纵轴差|`；当前小循环只展示距离，不消耗资源和时间。
- 青溪村可闭关，山野与遗址可探险；探险只会遭遇当前地点引用文件中的低阶灵兽。
- 敌对修士是另一类敌人资源；可交谈修士是角色资源，二者都不与灵兽池混用。
- 闭关或探险期间不能移动，必须先结束当前活动。

## 数据原则

- SQLite 是正式资产唯一事实来源，每次结算即时事务落库。
- JSON 用中文字段显式保存战斗属性、基础词条、功法、角色、敌人、物品、概率和数值，内容名直接作为 ID，
  便于直接查看与修改。
- 属性、资源、战斗流程和原子能力分别登记；机制、词条和功法分别保存，单个文件不再混装整个战斗系统。
- 功法的 `组成` 是完整能力树；通用校验器按 `原子能力.json` 校验字段和引用，运行时按执行器调度。
- 属性、效果类型和触发时机同样使用中文，不增加翻译表，也不兼容旧英文配置。
- 未知基础战斗效果必须阻止启动并指出具体配置路径，不能静默忽略。
- 玩家与敌对修士共用人物等级成长、功法、技能、被动、状态、冷却和用药结算；灵兽也走同一战斗流水线，
  但使用自身成长和基础攻击。敌人只在遭遇时生成临时战斗快照，不进入玩家数据库，奖励由发起遭遇的
  玩法组件处理。
- `基础数据/战斗` 是机制设计母本；正式运行只读取 `data`，未实现能力不能仅靠写入 JSON 假装可用。
- 不使用 Redis、消息队列、ORM、通用快照或小时级缓存回写。
