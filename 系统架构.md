# 晓楠修仙系统架构

## 原则

只保留三层：命令负责解析与回复，玩法组件负责规则和事务，核心只提供 SQLite、JSON、时间与战斗。
不建设通用命令总线、通用持续状态、主行动表、ORM、缓存服务、兼容层或万能快照。

## 目录

```text
game/cmd/<中文组件>/       命令登记、参数解析、玩家回复、活动守卫
game/features/<拼音领域>/  人物资产、地点、闭关、探险用例
game/rules/                不依赖消息和数据库的战斗规则
game/core/                 SQLite 事务、JSON 文件/分类目录递归读取、公共时间函数
game/content.py            分类中文 JSON 的展开、查重与领域校验
game/app.py                依赖装配
data/                      正式规则、分类内容与 SQLite 数据
log/                       框架文本日志
```

二级组件目录使用中文，玩法目录使用拼音。每个玩法组件拥有自己的表和规则，不再通过第二套
`CommandBus` 或万能服务转发。

## 调用链

```text
QQ / Local / 天道管理台
  -> launch 适配器
  -> game/cmd 活动守卫
  -> game/cmd 中文组件
  -> game/app.py 中注入的 feature
  -> 单个 SQLite 事务
  -> 公共消息对象
  -> 原适配器回复
```

三个入口共用同一命令、同一守卫和同一资产服务，不各自实现玩法分支。

运行 JSON 的字段、内容 ID、属性、效果和触发条件全部使用中文。内容名直接作为引用键，不建设
翻译表，也不保留旧英文 JSON 兼容层；Python 标识符和 SQLite 表结构继续使用英文。
分类目录可以继续分子目录，但加载后会抹平目录层级；因此整个 `data` 树中的 JSON 文件名必须全局唯一，
文件统一带类别前缀，例如 `功法-攻伐.json`、`方向-攻伐.json`、`物品-附魔-攻伐.json`。
功法和普通纳戒内容全部归入 `content/物品`。附魔、宝石文件同时承担物品底座与战斗能力定义，加载器
只生成运行期物品视图，不再维护第二份重复 JSON。

## 组件边界

### 人物资产 `game/features/player`

唯一负责 `players`、`weapons`、`inventory_stacks`、`techniques` 四张正式资产表。人物、本命
武器、纳戒、功法、经验、自动用药开关都在这里以单事务修改。活动组件不能绕过它直接拼资产 SQL。

### 闭关 `game/features/biguan`

`seclusion_states` 只保存用户、开始时间和随机种子。进度由开始时间实时计算；出关时在一个事务内
完成人物与同行伙伴的线性恢复、经验、人物功法生成和状态删除。60 分钟完成后不自动结算。

### 地点修士与伙伴 `game/features/xiushi`

`partner_relations` 保存好感、一次性回礼和同行关系；`partner_assets` 只保存首次入队生成的方向、资质、
当前资源、本命武器和 3/3/3 装配实例，不复制 JSON 规则正文。离队只切换关系，伙伴资产长期保留。

### 地点 `game/features/didian`

`player_locations` 每名人物只保存一条 `x/y` 真实位置，不保存展示地点名。世界边界、地点坐标、
当地功能与资源文件引用来自 `data/content/世界/青岚山境.json`；地点坐标必须唯一且处于世界边界内。
`修士池` 只引用 `content/角色/伙伴修士` 中按地点划分的文件，普通 `敌人池` 只引用
`content/敌人/灵兽` 文件。地点组件把多个文件中的
对象整合为候选池，触发玩法再直接从对象池抽取；不先抽文件，不让文件拆分方式暗中改变概率。
修士、敌人、物品、功法、品级、随机词条、附魔和宝石共用核心整数权重抽取器：能力或辅助价值越高的
底座通常权重越高、越稀有，抽中率与 `1 / 权重` 成正比；权重不参与战斗数值计算。
通用品级规则独立保存在 `data/rules/品级/品级-通用.json`。品级能力倍率作用于功法、附魔和宝石的
整棵能力树，价格系数作用于普通物品基础参考价，阶序负责品级范围比较。
地点不保存安全或危险字段，`可用功能` 是唯一业务入口：启用“修士”必须配置修士池，启用“探险”必须
配置敌人池，未启用对应功能也不得藏入资源池。敌对修士按流派保存在 `content/敌人/修士`，普通世界与
探险禁止引用。伙伴文件若配置战斗策略、功法、纳戒或掉落，类别校验会直接阻止启动。
地点组件是位置移动的唯一写入口，按曼哈顿距离计算移动距离，并允许即时前往任意已登记地点。
闭关和探险在各自事务内按当前坐标解析地点，不复制坐标或功能规则。

### 探险 `game/features/tanxian`

`exploration_states` 保存开始时间、人物资产修订号和最多六条精简轮次结果。轮次只保留敌人、胜负、
战后资源、临时状态、冷却、丹药消耗及奖励随机种子，不保存完整战斗日志。结束时只落账已解锁前缀；
人物或伙伴资产修订号不一致会拒绝覆盖。人物与同行伙伴组成同一阵营，各自使用自己的 3/3/3 装配，
整队共享人物纳戒丹药，伙伴本命武器同步获得探险经验。

### 战斗 `game/rules/combat.py` 与 `game/rules/battle`

纯规则模拟，不读写数据库。`combat.py` 负责编排行动、主动招式和自动丹药；`battle/damage.py` 只负责
固定伤害流水线；`battle/mechanics.py` 执行 JSON 机制、状态和事件触发；`battle/models.py` 保存输入、
状态和结构化战斗事实。功法能力树、随机词条、持续状态、冷却和连续遭遇均由调用方显式传入。

左右阵营的每名角色都使用 `CombatantSnapshot`，类别、等级、技能、被动、冷却与轮转游标归各自参战者所有。
同一阵营可以选择共享库存，但战斗结果仍逐人返回资源、状态和物品消耗。
玩家快照来自正式资产；敌人资源组件可生成灵兽或敌对修士。探险只选择低阶灵兽，敌对修士留给后续
秘境等独立玩法；角色修士不写入玩家资产表，掉落与奖励也不进入战斗核心。

战斗核心不认识具体功法 ID，也不按能力中文名分支。属性、资源、流程和原子能力来自战斗规则目录，
机制、词条、功法、附魔和宝石来自各自内容目录。角色方向只引用这些分类 JSON 文件名，加载后展开为
候选对象，不逐个重复抄写内容 ID，也不复制战斗规则。
`原子能力.json` 是能力 Schema，通用校验器据此递归检查字段、
类型、嵌套、引用和约束；运行期节点读取其中的执行器编号，再进入 Python 最小执行器注册表。

替换 JSON 后，只要继续组合已登记执行器支持的规则，就不需要修改 Python。只有增加一种底层从未实现的
结算语义时，才新增一个执行器；随后所有能力名、机制和功法都可以继续只写 JSON。

## 数据库

正式库 `data/game.db`：

```text
players             人物与当前资源
weapons             唯一本命武器
inventory_stacks    按用户、物品、品级联合堆叠的纳戒物品
techniques          功法实例与六个装配位
player_locations    人物当前 x/y 坐标
seclusion_states    闭关会话
exploration_states  探险预计算会话
partner_relations   伙伴好感、回礼和同行关系
partner_assets      伙伴持久资源、方向、本命武器与 3/3/3 装配
```

运行日志库 `data/runtime_log.db` 只有 `log_message_flows`。日志行自带创建和到期时间，由一个
30 分钟定时任务统一清理；删除整个日志库不会影响任何游戏资产。

SQLite 使用 WAL、外键、锁等待和明确事务。正式资产每次命令即时落库，不使用整点缓存回写，因此
进程退出不会丢失最近一小时的资产。

## 扩展方式

新内容优先增加或修改 `data` 中的 JSON。只有出现战斗核心尚不认识的底层语义时，才在
`game/rules/battle` 增加一种小而明确的执行器；字段规则继续写入 `原子能力.json`，不在
`game/content.py` 为某个能力增加专用校验分支。
新玩法使用独立 feature 和独立中文命令组件，不向闭关或探险塞无关分支。
