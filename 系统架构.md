# 晓楠修仙系统架构

## 一、地基原则

1. JSON 是游戏规则和正式内容的主体，Python 只负责读取、执行、状态事务和展示适配。
2. `game/core` 放跨玩法公共微服务，`game/features` 放具体玩法微服务，`game/cmd` 只负责命令、按钮和 HTTP 触发。
3. `game/app.py` 是唯一组合根，负责微服务创建顺序和依赖注入，不实现业务规则。
4. 目录规范、发布校核、评分、平衡模拟和内容生成只属于 `tools`；真实运行所需的跨服务启动契约统一属于 `game/startup`。
5. 当前不恢复旧人物、地点、探险、闭关、装配等业务，也不保留旧测试契约。
6. 框架配置只解释框架自身字段；数据库等游戏配置保留在 `config.custom`，由 `game/config.py` 解释和校验。
7. 新领域必须先完成主体规则、实体差异、读取规则和公共引用审查，再建立服务和测试；禁止由代码默认值替 JSON 补定义。

## 二、当前服务

```text
data JSON
  -> CoreServices.data      第一个公共微服务
  -> CoreServices.item_catalog  物品只读查询公共微服务
  -> CoreServices.combat    第二个公共微服务
  -> CoreServices.pool      第三个公共微服务
  -> CoreServices.world     第四个公共微服务
  -> CoreServices.database  第五个公共微服务
  -> CoreServices.player_state  玩家状态与命令守卫公共微服务
  -> CoreServices.asset          物资与资源状态公共微服务
  -> CoreServices.character      角色主体与创建编排公共微服务
  -> FeatureServices        创建人物、查看角色、查看物品、公开地图等玩法微服务
  -> game/cmd               只触发玩法微服务
```

### JSON 数据微服务

`game/core/data` 包含全部正式 JSON 读取能力：

- `files.py` 只固定读取 `data/定义/读取规则.json`，再按规则严格解析、匹配和归档全部正式 JSON。
- `loading.py` 根据规则声明的结构建立实体索引、编号池和源池，不按目录名称猜测业务类别。
- `service.py` 在进程启动时建立唯一不可变快照，并按数据集、实体类别和资源池向其他微服务提供查询。

所有正式 JSON 必须且只能匹配一条读取规则；未匹配文件、规则歧义、悬空池引用、池循环和编号前缀错误都会阻止启动。道路端点等世界业务约束由 `WorldService` 在消费数据集时校验，不进入通用读取器。旧的 `game/content.py`、`game/content_loading.py` 和 `game/core/data_files.py` 已删除，不保留兼容导入。运行期间不扫描、不监听、不重载 JSON；正式数据修改后必须重启服务。

### 战斗核心微服务

`game/core/combat` 是第二个公共微服务。它只消费已经初始化的 `JsonDataService` 提供的“战斗定义”“战斗规则”“战斗机制”和“战斗展示”，不得打开、遍历或硬编码 JSON 文件路径。

它负责行动条、技能冷却、伤害、状态、监听、事件链、召唤、构造物、整场模拟、结构化战报和展示协议。所有参战来源通过公共 `CombatantSpec` 和稳定构筑编号进入，内部转换为运行期快照，来源类型不改变底层结算语义。

器律与功法、真意、气机使用同一构筑装载协议。战斗核心只读取器律编号和能力，不解释兽宝、灵矿、铸法、器阶或覆炼。

战斗核心不负责：

- 创建人物、敌人、道侣或灵兽资产；
- 决定功法、真意、气机的获取、随机池和装配限制；
- 发放玩法奖励或写入玩法进度；
- 计算内容评分或修改平衡数值。

### 物品查询核心微服务

`game/core/item_catalog` 只消费 `JsonDataService` 已建立的 `物品` 实体索引，在启动时建立编号、名称和编号类别索引。编号查询必须精确；名称查询精确命中一个物品时返回详情，命中多个时返回候选编号，禁止凭相似名称猜测。

本服务返回物品正式定义中的公开字段：编号、名称、编号类别、说明、使用效果和强度。权重、参考价等游戏外维护字段不进入玩家查看结果；品级也不由定义查询服务补写，品级属于物品实例或具体业务状态。

`game/features/chakan_wupin` 是对应的只读玩法编排层，`game/cmd/查看` 只负责接收 `查看物品 编号/名称` 并渲染消息。查看物品不写数据库、不读取背包、不修改人物状态。

### 资源池微服务

`game/core/pool` 是第三个公共微服务。它消费 `JsonDataService` 已建立的内容池，统一处理允许重复和展开去重两种抽取模式，并执行唯一的逆权重算法。抽中量与 `1 / 权重` 成正比；权重只是抽样参数，不解释为评分、强度或稀有度。随机种子随结果返回，保证抽取可复现。

资源池服务不决定业务动机、不决定定向池、不写入角色资产。具体玩法只负责提交 `PoolRequest` 并处理只含编号和权重的 `PoolResult`。

### 世界地点微服务

`game/core/world` 根据正式世界 JSON 提供地点名或 `xy` 查询，统一计算区域、地形、海拔、地点功能和派生资源池。它不保存玩家位置，也不触发探险或行路业务。

`game/core/location` 独占玩家地表 `xy`、位置版本和附近坐标查询。`game/core/companion` 解释世界道侣的静态身份、等级境界和地点归属；玩家个人关系与同行实例不写回世界定义。

`game/features/ditu` 是对应的只读地图展示编排层，统一返回完整地图快照和由快照派生的文字概况。`game/cmd/地图` 只负责“地图”命令、公开页面和 HTTP 序列化，不直接导入世界核心服务。

### 核心数据库微服务

`game/core/database` 维护通用状态、幂等事务和唯一玩家位置三类事实。QQID直接作为 `user_id`；一般资产通过 `state_type + state_key` 扩展，只有需要 `xy -> 多玩家` 反向范围查询的位置使用结构化表。所有运行期读写使用包顶层异步接口，SQLite工作在线程中完成，不阻塞消息事件循环。

数据库只保证版本检查、幂等请求和多状态原子提交，不解释角色或物品 JSON。角色服务是玩家资产的唯一写入者；其他玩法提交结算结果，不得直接执行 SQL 或绕过角色服务修改玩家资产。

数据库对外返回的状态快照递归只读，调用方不能原地修改已经读取的事实。玩法若要改变状态，必须构造新的状态值并通过带版本号的事务提交。

### 玩家状态核心微服务

`game/core/player_state` 解释正式 JSON 中的行为、队伍、控制三类状态，以及状态转换和命令守卫规则。状态事实保存为 `player_state/main`；本服务只校验和保存状态，不实现休息收益、队伍成员、托管循环、行路计时或战斗结算。

所有游戏命令必须显式声明一个已有守卫规则。`game/startup` 在服务装配完成后一次性核对全部命令引用，存在未知规则时拒绝启动。

### 启动契约

`game/startup` 是跨命令、跨核心服务运行契约的唯一位置。`contracts.py` 统一检查命令注册唯一性、数据库状态唯一写入归属和命令守卫引用；`__init__.py` 只导出公共入口。`game/app.py` 只在服务装配完成后调用一次，不保存具体检查实现。

检查文件按性质归属：单个微服务自己的 JSON 字段、索引和运行数据校验留在所属微服务；跨模块且运行前必须成立的契约放在 `game/startup`；目录、命名、`scope` 分类和发布完整性等维护审查放在 `tools`。测试只能调用这些正式检查入口，不得另建一套重复规则。

### 角色核心微服务

`game/core/character` 负责人物创建与玩家资产的统一状态入口，解释人物主体、性别、境界、初始构筑和初始物资，并通过核心数据库原子写入人物、背包、初始状态与初始位置。角色资产不复制 `xy`；具体玩法不得绕过角色服务或位置服务直接修改对应事实。

### 玩法微服务

`game/features` 只放具体玩法微服务。每个微服务通过构造参数声明依赖，只能由 `build_game_services()` 装配，不能从全局位置临时创建核心服务。当前已有创建人物、人物总览、物品查询、地图、纳戒、行路和位置查看；后续玩法继续从正式 JSON 契约逐项重建。

### 触发层

`game/cmd` 是命令与 HTTP 入口总聚合；`.env` 只把根包登记为一个路由模块，根包再通过统一导入函数一次加载全部二级组件，并收集各组件公开的 `router`，不得重复扫描子包或逐个编写中文模块导入。`game/cmd/天道后台` 独占开发者管理台，`game/cmd/地图` 独占公开地图命令与只读页面。文本回调只通过三种注册器进入：`fullmatch` 完整匹配整条消息，`command` 匹配命令词与参数，`regex` 匹配完整消息正则。QQ 按钮由适配器处理，不增加第四种游戏回调注册器。触发层只调用已经装配好的玩法服务，不直接导入核心微服务、不读取 JSON、不组装业务。

消息身份和目标统一遵守以下命名：QQ 原始 payload 的 `user_openid`、`member_openid`、`group_member_openid` 在解析边界统一为 `user_id`，`group_openid` 统一为 `group_id`；驱动器对象、公共消息上下文、业务命令和数据库始终使用同一套名称。`sender_name` 只表示当前会话展示名，不参与身份识别。回复使用 `ReplyTarget.user_id + ReplyTarget.target_id`，业务调用 `manager.send(message)` 时默认使用当前 `reply_target`。

编号必须按所有者命名，禁止重新引入没有语义的 `identity` 或裸 `id` 字段：平台事件使用 `event_id`，平台消息使用 `message_id`，业务幂等请求使用 `request_id`，消息动作使用 `action_id`；JSON 实体、物品和战斗构筑分别使用 `entity_id`、`item_id`、`content_id`，阵法和丹药分别使用 `formation_id`、`medicine_id`。QQ 的 `interaction_id` 只表示一次按钮点击事件，不能替代 `action_id`。地表位置统一使用 `xy`，性别、参战者类别和战斗对象类别分别使用 `gender`、`combatant_type`、`object_type`。

QQ 与 Local 各自维护命令索引、消息会话、排队和回复状态。`launch/adapter` 只定义公共生命周期、注册器、消息上下文和守卫契约，不提供共享会话运行时。QQ 自己的命令规则、后台队列与事件去重分别放在 `launch/adapter/qq/rules.py`、`runtime.py` 和 `dedupe.py`。

每个命令二级组件以 `__init__.py` 作为唯一回调注册入口。回调只能把文本转换为玩法公共请求、调用一个明确玩法入口并发送回复；不得根据核心状态版本、编号关系或资产明细推断业务事实。复杂回复放入按需存在的 `reply.py`，复杂命令语法放入按需存在的 `input.py`，两者都不得取得游戏服务。回调继续归属于组件包模块，因此沿用现有命令、帮助、QQ 和 Local 精确模块替换与卸载契约，不修改通信框架。

## 三、装配顺序

`game/app.py` 固定按以下顺序启动：

1. `game/config.py` 从框架自定义项取得并校验游戏配置。
2. 创建并初始化 `JsonDataService`。
3. 把数据服务注入 `CombatService` 并初始化战斗基石与战报目录。
4. 把数据服务注入 `ItemCatalogService` 并建立物品查询索引。
5. 把数据服务注入 `PoolService` 并启用两种抽取模式。
6. 把数据服务注入 `WorldService` 并建立地点、区域和地表索引。
7. 创建 `DatabaseService`，建立状态、事务和玩家位置表，不执行旧代码迁移。
8. 把数据与数据库服务注入 `AssetService`，登记物资状态所有权。
9. 把数据与数据库服务注入 `PlayerStateService`，加载三类状态和守卫规则。
10. 把数据、数据库、资产与状态服务注入 `CharacterService`，建立角色主体和创建编排入口。
11. 创建已完成准入审查的 `FeatureServices`，当前包含创建人物、查看角色、查看物品和公开地图。
12. 通过 `game/startup` 一次校验命令唯一性、状态唯一归属和全部命令守卫引用。
13. 注册统一游戏守卫后才对外提供服务。
14. 由 `game/cmd` 中的触发器调用对应玩法服务，玩法服务再使用注入的核心服务。

只有启动回调可以构造游戏服务；`current_game_services()` 只返回已经装配完成的实例，启动前调用会直接报错。任何核心服务初始化失败都阻止游戏启动，不向后续服务暴露半加载状态。

框架生命周期按“驱动器、调度器、业务服务”启动，并按相反顺序清理。每项资源在启动前登记自己的关闭动作；任一步失败都继续关闭已进入启动阶段的资源，单项关闭失败不得阻断后续清理。

## 四、依赖方向

```text
game/cmd ---------> game/features          只触发具体玩法
                         |
                         v
game/app.py ------> game/core/*            按具体微服务包注入
   |
   +-------------> game/startup            统一运行时启动契约
   |
   +-------------> launch.OnEvent          生命周期注册契约
   |
   +--> game/features
   |
   +--> game/core/combat
   |          |
   |          v
   +--> game/core/data --> data JSON
   |
   +--> game/core/database --> database/game.db

launch -X-> game                           框架不得静态引用应用层
game/config.py ---> launch.config.custom   游戏解释自定义配置
tools  ---> data JSON / core
game   -X-> tools
core   -X-> features / cmd
```

`game/core/__init__.py` 不转发具体服务。其他业务只能从各核心微服务包顶层导入公共契约；禁止导入服务内部文件。完整规则见 `微服务边界规范.md`。

`launch/load_router.py` 只执行配置声明的模块装载计划，不硬编码任何 `game` 模块；源码依赖必须始终从应用层指向框架公共契约。

依赖若反向，或者出现第二套 JSON Reader、第二套战斗入口、评分进入运行时，应直接视为架构错误。

## 五、历史清理边界

- 旧 `game/content.py`、`game/content_loading.py`、`game/core/data_files.py`；
- 旧 `game/rules` 全部源码；
- 重构前的 `tests` 契约；
- 重构前的人物、构筑、世界、战场、行程、物品、炼器和炼药二级组件实现；
- 重构前的人物、地点、敌人、闭关、探险和装配业务实现。

上述内容是已经完成的历史清理，不是当前待办。当前角色、世界、位置、队伍、道侣、培养、探险、闭关、采集、炼器、炼丹、阵法、交易、服丹和特殊地点玩法均已按新边界重建；后续功能必须继续复用现有核心契约，不得恢复旧实现或兼容路径。
