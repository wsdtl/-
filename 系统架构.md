# 晓楠修仙系统架构

## 一、当前原则

本轮重构只保留可复用底座，不保留旧业务实现。

1. JSON 是游戏规则和内容的主体，代码只是读取器、执行器、状态事务和展示适配器。
2. 战斗核心只实现 JSON 已声明的通用原子语义，不内置某门功法、某颗宝石、某个附魔或某类角色的专用规则。
3. 控制台和战报系统属于基础设施，予以保留；它们不能反向拥有游戏业务规则。
4. 校核、评分、平衡模拟和内容生成属于游戏外工具，只能放在 `tools/` 或维护脚本中。
5. 当前没有人物、修士、地点、闭关、探险、纳戒等正式业务服务。后续逐项重建时必须从 JSON 契约出发。

## 二、保留层次

```text
正式 JSON
  -> JsonDataReader / GameDataLoader
  -> GameContent 运行视图
  -> BattleEngine 通用战斗执行
  -> BattleReport 结构化战报
  -> 控制台、静态战报页或其他入口展示
```

### 正式 JSON

`data/定义`、`data/规则`、`data/内容`、`data/展示` 共同构成正式数据。文件身份、稳定编号和池引用负责定位对象；展示名称不承担数据库主键职责。运行时只读取发布后的可信 JSON，不运行全库评分或平衡审查。

### 基础游戏核心

`game/core` 只提供严格 JSON 读取、SQLite 事务、公共时间和资源池等基础能力。它不判断角色如何成长，也不负责某项玩法的奖励或流程。

### 内容加载

`game/content_loading.py` 负责索引实体、解析编号、展开引用和资源池。`game/content.py` 提供战斗执行当前需要的内容视图。两者都不得把文件名已经表达的分类再次硬编码为重复业务正文，也不得引入评分依赖。

### 战斗核心

`game/rules/combat.py` 与 `game/rules/battle` 负责行动条、技能冷却、伤害、状态、监听、召唤、构造物、事件链和整场模拟。所有参战对象统一转换为 `CombatantSnapshot`；角色来源不改变底层结算语义。

战斗核心不负责：

- 创建人物、敌人或道侣资产；
- 决定功法、附魔、宝石如何随机分池；
- 发放探险奖励或写入玩法进度；
- 计算内容评分或自动修改平衡数值。

### 战报系统

战报是战斗结果的通用投影。`game/rules/battle/report.py` 生成结构化记录，`static/battle-report` 负责展示，`scripts/generate_battle_report_demo.py` 提供独立演示。战报只消费战斗事实，不依赖任何具体玩法服务。

### 控制台

`game/public` 与 `static/game-console` 保留为通用运维和交互外壳。控制台可以展示消息、调用以后重新建立的入口，但不能保存另一套人物、战斗或内容规则。该包只注册公共服务触发器；`game/cmd` 以后只注册具体玩法命令。两者在 `.env` 中作为独立路由组加载，不能互相聚合。

## 三、已移除边界

以下内容不再属于当前代码事实：

- `game/app.py` 旧业务依赖装配；
- `game/features` 下的人物、修士、地点、敌人、闭关、探险和装配服务；
- `game/cmd` 下对应的旧业务命令；
- 依赖上述服务的数据迁移测试和业务说明。

数据库中即使仍存在历史表或数据，也不能被视为当前架构契约。后续存储模型应在相应业务重新设计时确定。

## 四、未来扩展规则

新增或修改内容时，优先修改 JSON。只有 JSON 需要一种现有战斗核心无法表达的底层语义时，才增加一个小而通用的执行器，并同步补充原子声明、边界测试和战报表达。

重建业务服务时遵守以下方向：

- 服务读取稳定编号和池引用，不复制 JSON 内容正文；
- 存档保存身份、实例状态和必要随机结果，不保存可由正式 JSON 重建的规则；
- 功法、附魔、宝石保持独立方向，组合发生在装配或战斗输入阶段；
- 业务服务可以调用战斗核心，战斗核心不得导入业务服务；
- 控制台与战报可以观察或展示业务结果，不能成为规则来源。

## 五、允许的依赖方向

```text
launch / message / console
          |
          v
未来业务服务（当前为空）
          |
          v
content loading + core + battle rules
          |
          v
        data JSON

tools -> data JSON / core / battle rules
game  -X-> tools
battle rules -X-> future business services
```

入口注册保持分组：

```text
game.cmd                       具体玩法命令
game.public                    控制台、战报等公共服务触发器
```

这个依赖方向是后续重构的硬边界。任何新代码若把内容规则写回 Python、让战斗核心依赖具体玩法，或让评分进入运行时，都应直接拒绝合并。
