# 晓楠修仙系统架构

## 一、地基原则

1. JSON 是游戏规则和正式内容的主体，Python 只负责读取、执行、状态事务和展示适配。
2. `game/core` 放跨玩法公共微服务，`game/features` 放具体玩法微服务，`game/cmd` 只负责命令、按钮和 HTTP 触发。
3. `game/app.py` 是唯一组合根，负责微服务创建顺序和依赖注入，不实现业务规则。
4. 校核、评分、平衡模拟和内容生成只属于 `tools`，不得进入游戏运行链路。
5. 当前不恢复旧人物、地点、探险、闭关、装配等业务，也不保留旧测试契约。

## 二、当前服务

```text
data JSON
  -> CoreServices.data      第一个公共微服务
  -> CoreServices.combat    第二个公共微服务
  -> CoreServices.pool      第三个公共微服务
  -> FeatureServices        以后逐项增加玩法微服务
  -> game/cmd               命令、按钮和 HTTP 触发
```

### JSON 数据微服务

`game/core/data` 包含全部正式 JSON 读取能力：

- `files.py` 严格解析 `data/定义`、`data/规则`、`data/内容`、`data/展示`。
- `loading.py` 建立文件注册表、内容分组和编号实体索引。
- `service.py` 在进程启动时建立唯一只读快照，并向其他微服务提供查询。

旧的 `game/content.py`、`game/content_loading.py` 和 `game/core/data_files.py` 已删除，不保留兼容导入。运行期间不扫描、不监听、不重载 JSON；正式数据修改后必须重启服务。

### 战斗核心微服务

`game/core/combat` 是第二个公共微服务。它只消费已经初始化的 `JsonDataService`，不得打开或遍历 JSON 文件。

它负责行动条、技能冷却、伤害、状态、监听、事件链、召唤、构造物、整场模拟、结构化战报和展示协议。所有参战来源统一转换为 `CombatantSnapshot`，来源类型不改变底层结算语义。

战斗核心不负责：

- 创建人物、敌人、道侣或灵兽资产；
- 决定功法、附魔、宝石的获取、随机池和装配限制；
- 发放玩法奖励或写入玩法进度；
- 计算内容评分或修改平衡数值。

旧的 `game/rules` 已删除，不保留转发入口。

### 资源池微服务

`game/core/pool` 是第三个公共微服务。它消费 `JsonDataService` 已建立的内容池，统一处理允许重复和展开去重两种抽取模式，并执行唯一的逆权重算法。权重数值越高越稀有，抽中概率与 `1 / 权重` 成正比；随机种子随结果返回，保证抽取可复现。

资源池服务不决定业务动机、不决定定向池、不写入角色资产。具体玩法只负责选择池和处理 `PoolResult`。

### 玩法微服务

`game/features` 只放具体玩法微服务。每个微服务通过构造参数声明依赖，只能由 `build_game_services()` 装配，不能从全局位置临时创建核心服务。当前目录只有骨架，后续从正式 JSON 契约逐项重建。

### 触发层

`game/cmd` 是命令与 HTTP 入口总聚合，`game/cmd/public` 放控制台、战报等公共入口。触发层通过 `current_game_services()` 调用已经装配好的服务，不读取 JSON，不实现游戏业务。

## 三、装配顺序

`game/app.py` 固定按以下顺序启动：

1. 创建并初始化 `JsonDataService`。
2. 把数据服务注入 `CombatService` 并初始化战斗基石与战报目录。
3. 把数据服务注入 `PoolService` 并启用两种抽取模式。
4. 创建 `FeatureServices`，以后新增玩法服务时显式注入所需核心服务。
5. 由 `game/cmd` 中的触发器调用服务。

任何核心服务初始化失败都直接阻止游戏启动，不向后续服务暴露半加载状态。

## 四、依赖方向

```text
launch / message
       |
       v
game/cmd
       |
       v
game/app.py
       |
       +--> game/features
       |
       +--> game/core/combat
       |          |
       |          v
       +--> game/core/data
                  |
                  v
               data JSON

tools -> data JSON / core
game  -X-> tools
core  -X-> features / cmd
```

依赖若反向，或者出现第二套 JSON Reader、第二套战斗入口、评分进入运行时，应直接视为架构错误。

## 五、当前删除项

- 旧 `game/content.py`、`game/content_loading.py`、`game/core/data_files.py`；
- 旧 `game/rules` 全部源码；
- 旧 `tests` 全部契约；
- 旧人物、地点、敌人、闭关、探险和装配业务实现。

这是后续重建玩法服务的唯一地基。
