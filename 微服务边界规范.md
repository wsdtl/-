# JSON 微服务边界规范

## 一、定义

本项目中的微服务首先指代码级服务边界：一类业务拥有独立目录、公共契约、公共服务门面和内部运行实现。它不默认要求独立进程、网络协议或 HTTP。

JSON 是正式定义源；稳定编号与公共契约是微服务之间的通信语言。其他业务不得通过传递内部字典、运行期对象或导入引擎模块来调用服务。

## 二、目录规则

每个微服务目录统一承担以下职责：

```text
service-name/
  __init__.py     唯一公共导出清单
  contracts.py    请求、结果、状态和公共错误
  service.py      公共服务门面
  说明.md          数据所有权、依赖和调用示例
  其他文件         服务内部实现，不属于公共接口
```

只有包顶层公开的名称可被其他运行时业务使用。`engine`、`models`、`runtime`、`schema`、`selection`、`loading`、`files` 等实现模块不得被跨服务导入。

## 三、依赖方向

```text
cmd -> features -> core services -> JsonDataService -> data JSON
               -> persistence repositories

game/app.py -> 创建并注入全部服务
tools       -> 可以为校核和基准访问内部实现
```

- `cmd` 只负责触发和输入输出，不得直接导入核心服务。
- `features` 可以从核心微服务包顶层导入公共契约和服务类型，不得导入其子模块。
- 核心服务不得引用 `features` 或 `cmd`。
- `game/core/__init__.py` 只保留核心命名空间版本，不转发具体服务。
- `game/app.py` 从具体微服务包导入并完成依赖注入。

## 四、JSON 所有权

`JsonDataService` 只按 `data/定义/读取规则.json` 加载、归档、索引和返回正式 JSON，不解释玩法语义。目录本身表达的归属也必须由读取规则转成实体元数据。每个微服务通过注入的数据服务按数据集名、实体类别或资源池读取并解释自己拥有的 JSON，不得在自身代码中遍历或拆解数据目录。

调用方应提交稳定编号和业务状态。例如战斗构筑提交功法、附魔、宝石和器律编号，由战斗服务读取实体并组装运行期能力；资源池返回抽中的编号和权重，不把整份实体定义复制给调用方。

## 五、公共结果

公共结果必须与内部运行对象隔离：

- 不得返回 `Fighter`、`BattleContext`、`StatusState` 等可变运行对象。
- 不得要求调用方补交本服务执行时已经拥有的数据。
- 公共请求和结果改动需要显式调整契约，不以任意新增字典键代替版本管理。
- 随机服务必须返回或接受种子，保证结果可复现。

## 六、现有服务

### JSON 数据

```python
from game.core.data import JsonDataService
```

公开数据集、实体和资源池展开能力；按路径读取、文件扫描、规则匹配与加载阶段属于内部实现。

### 资源池

```python
from game.core.pool import EXPAND_DEDUPLICATED, PoolRequest

result = pool.draw(
    PoolRequest(
        section="功法",
        count=3,
        mode=EXPAND_DEDUPLICATED,
        full_pool=True,
        seed=1001,
    )
)
```

结果只返回稳定编号和权重。

### 战斗

```python
from game.core.combat import CombatRequest

result = await combat.execute(
    CombatRequest(
        left_team=(player,),
        right_team=(enemy,),
        seed=1001,
        action_limit=2000,
    )
)
```

`CombatantSpec.build` 只保存功法、附魔、宝石和器律编号及实例信息；战斗服务自行解析 JSON 并执行。

### 炼器

```python
from game.core.forge import ForgeRequest, WeaponState

plan = forge.plan(
    ForgeRequest(
        law_id="700001",
        slot=1,
        weapon=WeaponState(level=11),
        guides=guides,
        auxiliaries=minerals,
    )
)
```

炼器服务解释兽引、铸脉、铸法和器律，返回精确耗材方案与覆炼后的不可变武器状态。战斗服务只读取最终器律编号，不导入炼器内部实现，也不接触兽宝和灵矿。

### 当前所有权

| 服务 | 独占解释 | 可以依赖 |
| --- | --- | --- |
| `data` | 读取规则、文件匹配、冻结、实体元数据和资源池索引 | 无其他游戏服务 |
| `combat` | 战斗定义、战斗规则、机制、环境阶段和二次战斗对象执行 | `data` |
| `pool` | 候选展开后的逆权重抽取与随机复现 | `data` |
| `build` | 功法、附魔、宝石槽位生成与机制相冲 | `data`、`pool` |
| `world` | 世界、区域、地点、地势和道路事实 | `data` |
| `battlefield` | 地点地形或秘境到环境编号的选择 | `data`、`world`、`combat` 公共战场契约 |
| `travel` | 自动选路、行程指标和行程叙事 | `data`、`world` |
| `item` | 物品分类、物品定义和使用效果 | `data` |
| `forge` | 兽引、铸脉、铸法、器律和本命武器成长 | `data`、`item` |
| `role` | 人物、道侣、敌人、成长和角色构筑需求 | `data`、`item`、`forge` |
| `alchemy` | 药脉、炉法、丹方、耗材和战丹准备 | `data`、`item` |

`阵法规则`、`阵法` 和 `玩法规则` 当前只有数据加载边界，没有领域解释服务。其他服务不得为了临时使用而读取并解释这些数据；应在对应玩法施工时建立独立服务并由 `game/app.py` 注入。

## 七、自动守卫

`tests/test_microservice_boundaries.py` 检查公共导出和运行时代码导入方向。出现跨服务内部导入时，测试必须失败，不能以临时兼容导入绕过。
