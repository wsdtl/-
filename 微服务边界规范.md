# JSON 微服务边界规范

## 一、定义

本项目中的微服务首先指代码级服务边界：一类业务拥有独立目录、公共契约、公共服务门面和内部运行实现。它不默认要求独立进程、网络协议或 HTTP。

JSON 是正式定义源；稳定编号与公共契约是微服务之间的通信语言。其他业务不得通过传递内部字典、运行期对象或导入引擎模块来调用服务。

## 二、目录规则

每个微服务目录统一承担以下职责：

```text
service-name/
  __init__.py     唯一公共导出清单
  contracts.py    请求、结果、状态和公共错误
  service.py      公共服务门面
  说明.md          数据所有权、依赖和调用示例
  其他文件         服务内部实现，不属于公共接口
```

只有包顶层公开的名称可被其他运行时业务使用。`engine`、`models`、`runtime`、`schema`、`selection`、`loading`、`files` 等实现模块不得被跨服务导入。

## 三、依赖方向

```text
cmd -> features -> core services -> JsonDataService -> data JSON
               -> persistence repositories

game/app.py -> 创建并注入全部服务
tools       -> 可以为校核和基准访问内部实现
```

- `cmd` 只负责触发和输入输出，不得直接导入核心服务。后台命令只能由后台 scope 注册，必须隐藏且不得发布帮助。
- `features` 可以从核心微服务包顶层导入公共契约和服务类型，不得导入其子模块。
- 核心服务不得引用 `features` 或 `cmd`。
- `game/core/__init__.py` 只保留核心命名空间版本，不转发具体服务。
- `game/app.py` 从具体微服务包导入并完成依赖注入。服务重启时，注册器按来源模块替换旧注册；显式卸载时同步移除命令、帮助、QQ 和 Local 回调，避免重复回调。

`cmd` 的处理器只能消费 `features` 已经给出的明确业务事实。装配数量、关系是否建立、是否同行、能否执行、资产变化和状态转换等结论必须由玩法公共结果提供；命令层不得从版本号、稳定编号或底层状态正文再次推断。命令组件的 `reply.py` 与 `input.py` 是纯适配模块，不得取得组合根服务。

## 四、JSON 所有权

`JsonDataService` 只按 `data/定义/读取规则.json` 加载、归档、索引和返回正式 JSON，不解释玩法语义。目录本身表达的归属也必须由读取规则转成实体元数据。每个微服务通过注入的数据服务按数据集名、实体类别或资源池读取并解释自己拥有的 JSON，不得在自身代码中遍历或拆解数据目录。

调用方应提交稳定编号和业务状态。例如战斗构筑提交功法、真意、气机和器律编号，由战斗服务读取实体并组装运行期能力；资源池返回抽中的编号和权重，不把整份实体定义复制给调用方。

## 五、公共结果

公共结果必须与内部运行对象隔离：

- 不得返回 `Fighter`、`BattleContext`、`StatusState` 等可变运行对象。
- 不得要求调用方补交本服务执行时已经拥有的数据。
- 公共请求和结果改动需要显式调整契约，不以任意新增字典键代替版本管理。
- 随机服务必须返回或接受种子，保证结果可复现。

## 六、现有服务

### JSON 数据

```python
from game.core.data import JsonDataService
```

公开数据集、实体和资源池展开能力；按路径读取、文件扫描、规则匹配与加载阶段属于内部实现。

### 资源池

```python
from game.core.pool import EXPAND_DEDUPLICATED, PoolRequest

result = pool.draw(
    PoolRequest(
        section="功法",
        count=3,
        mode=EXPAND_DEDUPLICATED,
        full_pool=True,
        seed=1001,
    )
)
```

结果只返回稳定编号和权重。

### 战斗

```python
from game.core.combat import CombatRequest

result = await combat.execute(
    CombatRequest(
        left_team=(player,),
        right_team=(enemy,),
        seed=1001,
        action_limit=2000,
    )
)
```

`CombatantSpec.build` 只保存功法、真意、气机和器律编号及实例信息；战斗服务自行解析 JSON 并执行。

### 世界

```python
from game.core.world import LocationQuery

location = world.locate(LocationQuery(xy=(10, 20)))
```

世界服务只返回地点、区域、地形、海拔和可用功能，不保存玩家位置。

### 核心数据库

```python
from game.core.database import StateMutation, TransactionCommand

receipt = await database.commit(
    TransactionCommand(
        user_id=user_id,
        request_id=request_id,
        business_type="创建人物",
        mutations=(StateMutation("character", "main", state, 0),),
        payload={},
    )
)
```

数据库只处理状态版本、幂等请求和事务原子性。后续角色服务是玩家资产唯一写入者，命令和其他玩法不得直接写数据库。

### 当前所有权

| 服务 | 独占解释 | 可以依赖 |
| --- | --- | --- |
| `data` | 读取规则、文件匹配、冻结、实体元数据和资源池索引 | 无其他游戏服务 |
| `combat` | 战斗定义、战斗规则、机制、环境阶段和二次战斗对象执行 | `data` |
| `pool` | 候选展开后的逆权重抽取与随机复现 | `data` |
| `world` | 地点、区域、地形、海拔与派生地点内容 | `data` |
| `database` | 两表状态快照、版本检查、幂等事务与原子提交 | 无其他游戏服务 |
| `item_catalog` | 物品编号、名称、类别与公开定义查询 | `data` |
| `player_state` | 多类型玩家状态、状态转换与命令守卫规则 | `data`、`database` |
| `character` | 人物创建、人物资产初始状态与玩家资产写入口 | `data`、`database`、`player_state` |

当前已建立 `chuangjian_renwu`、`chakan_juese`、`chakan_wupin` 与 `ditu` 四个玩法微服务，分别编排角色、玩家状态、世界、物品查询和公开地图展示。构筑、行程、炼器、炼药、阵法及其他具体玩法仍只有正式数据边界；任何调用方不得为了临时使用而自行解释这些 JSON，应在该领域主体规则完成审查后建立独立服务并由 `game/app.py` 注入。

## 七、新服务准入

新服务必须依次通过以下事实检查，不能从代码或测试倒推 JSON：

1. 主体 JSON 已写明该类对象共同拥有的规则和引用。
2. 世界或内容实体只保存个体差异，不复制主体公共字段，也不依赖代码补默认值。
3. `data/定义/读取规则.json` 已经把相关文件纳入明确的数据集、结构和索引。
4. `JsonDataService` 能独立加载全部正式 JSON，并拒绝缺失引用、重复编号和规则歧义。
5. 前四项成立后，才设计公共契约、服务实现和该领域的新测试。

测试通过只能证明实现满足已经声明的契约，不能证明 JSON 主体完整。以后发现主体缺失时，先修主体与读取规则，再修改服务；不得在组件内增加隐式随机、回退值或兼容分支掩盖数据缺口。

## 八、动态导入

运行时动态导入只允许出现在两个装载边界：`game/cmd/__init__.py` 负责按声明装载命令包，`launch/load_router.py` 负责按配置装载路由。其他 `game/`、`launch/` 代码不得调用 `import_module`、`importlib.import_module` 或 `__import__`；新增例外必须先修改架构契约和审查测试。
