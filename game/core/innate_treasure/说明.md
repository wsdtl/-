# 先天灵宝核心

本服务解释正式先天灵宝实体、玩家永久灵宝谱和唯一执掌槽。状态类型固定为 `innate_treasure`，不进入纳戒，也不允许道侣、交易、宗门仓储或掉落服务写入。

其他业务只按公开的规则节点查询当前灵宝效果。持续玩法必须在开始时保存灵宝编号，结算时按快照解释；瞬时玩法可以读取当前执掌。服务不实现秘境掉落，后续秘境只能通过 `plan_acquire()` 把取得结果纳入自己的原子结算。
