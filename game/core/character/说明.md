# 角色核心微服务

角色服务解释 `data/规则/角色`、`data/定义/角色`、战斗属性、境界、初始炼器规则，并拥有玩家角色状态的唯一写入权限。

创建人物是一笔数据库事务，写入 `character/main`、`cultivation/main`、`weapon/main` 和初始丹药背包状态。服务只接收稳定用户编号、创建输入和出生地 `xy`，不读取命令文本，也不直接访问 JSON 文件系统。
