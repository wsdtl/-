# 托管核心

本核心解释托管 JSON、保存唯一活动计划、原子切换参与者控制状态，并向本地驱动器一次提供一个已认领命令。它不导入探险、闭关、采药或采矿核心，也不直接结算任何玩法。

计划阶段固定为`待开始`、`执行开始`、`待结束`和`执行结束`。认领时生成稳定请求编号；进程在命令执行后、计划推进前停止时，重启会使用同一请求编号重放，依靠各玩法事务保证不重复消耗和结算。

开始步骤成功的标准是所有锁定参与者进入该活动的行为状态；结束步骤成功的标准是所有参与者回到该行为定义的结束状态。任何命令未命中、业务拒绝、同行变化或状态不一致都会保存原因并暂停，不会跳过活动。
