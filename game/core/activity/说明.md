# 人物行为状态核心微服务

本服务负责解释 `data/规则/角色/状态/人物状态.json`，读取和变更玩家当前行为状态。状态正文保存在数据库 `state_snapshot` 的 `character_status/main`，不新增物理表。

状态规则包括三部分：初始状态、状态之间允许的转入关系、命令准入规则。命令层只把命令声明的准入规则交给本服务，不能直接读取数据库或判断状态名称。

创建人物时，角色服务把 `ActivityService.initial_mutation()` 合并进创建人物事务，因此人物、修行槽、本命武器、背包和初始状态要么全部写入，要么全部回滚。
