# 物品查询核心微服务

`item_catalog` 只消费已经由 `game.core.data.JsonDataService` 加载的正式 JSON 快照，启动时建立物品编号索引、名称索引和类别索引。

本服务不访问数据库、不读取背包、不解析 QQ 消息，也不负责发送消息。编号是稳定主键，名称只是查询入口；权重、参考价等维护字段不会通过玩家查看命令暴露。

物品定义的品级不在本服务中补写。当前 JSON 物品实体没有品级字段，品级属于背包实例或业务产生的物品状态。
