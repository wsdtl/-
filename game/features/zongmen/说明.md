# 宗门关系玩法

本组件只负责把角色名或 `user_id` 解析为宗门核心调用，并依据 `data/展示/宗门` 生成回复。宗门主体、成员、邀请和入口的读写全部归 `game/core/sect`，本组件不直接拼装数据库状态。

当前开放创建、查看、邀请、接受、拒绝、退出、逐出、转让宗主、解散、宗门同行和山门进出。山门入口进入本宗独立洞天，宗主带领当前宗门同行成员一同切换空间；洞天内禁止世界行路，附近人物按空间隔离。建筑、仓库、资源、任务和宗门战尚未实现。
