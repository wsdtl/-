# 闭关组件

闭关固定 60 分钟，每满 10 分钟完成一轮。出关时按真实秒数线性恢复人物资源，并按完整轮次结算
人物经验和随机功法。表中只保存开始时间与随机种子；结算成功后删除会话。命令和守卫位于
`game/cmd/闭关`。

时间、经验、感悟概率和圆满清理规则从 `data/rules/修炼.json` 的 `闭关` 中文节点读取；功法
和品级从 `data/content/功法.json` 读取，随机词条再从每本功法引用的
`data/content/词条.json` 基础词条池中抽取。
