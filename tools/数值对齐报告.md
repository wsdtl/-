# 旧版数值对齐报告

## 范围

本报告只记录从 `C:\Users\16841\Desktop\xiuxian3` 读取的物价和数值锚点。旧项目不参与运行，不复制旧项目的目录、命令、数据库、业务流程、实体名称或实现代码。

本轮迁移的是货币单位、成长曲线、玩法时长和交锋收益的宏观尺度，不逐项复制旧实体。当前项目自己的名称、编号、分类、机制和目录结构保持不变。

## 已直接对齐

| 当前 JSON | 字段 | 对齐值 | 旧版来源 |
| --- | --- | ---: | --- |
| `data/定义/战斗/属性.json` | 血气上限默认值 | `100` | `基础数据/人物属性默认值.json` |
| `data/定义/战斗/属性.json` | 精神上限默认值 | `60` | `基础数据/人物属性默认值.json` |
| `data/规则/角色/主体/人物.json` | 血气上限初始覆盖 | `100` | `基础数据/人物属性默认值.json` |
| `data/规则/角色/主体/人物.json` | 精神上限初始覆盖 | `60` | `基础数据/人物属性默认值.json` |
| `data/规则/角色/主体/人物.json` | 攻击初始覆盖 | `5` | `基础数据/人物属性默认值.json` |
| `data/规则/角色/主体/人物.json` | 初始灵石 | `10000` | 旧版新手货币尺度 |
| `data/规则/角色/成长/修士修炼.json` | 每级血气上限 | `12` | `基础数据/人物成长/每级成长.json` |
| `data/规则/角色/成长/修士修炼.json` | 每级精神上限 | `6` | `基础数据/人物成长/每级成长.json` |
| `data/规则/角色/成长/修士修炼.json` | 每级攻击 | `0.1` | `基础数据/人物成长/每级成长.json` |
| `data/规则/角色/成长/修士修炼.json` | 每级防御 | `1` | `基础数据/人物成长/每级成长.json` |
| `data/规则/角色/成长/修士修炼.json` | 1 至 100 级累计经验 | `351398872` | 旧版人物经验曲线 |
| `data/规则/炼器/本命武器.json` | 等级上限 | `100` | `基础数据/武器/武器成长.json` |
| `data/规则/炼器/本命武器.json` | 每级攻击 | `1` | `基础数据/武器/武器成长.json` |
| `data/规则/炼器/本命武器.json` | 1 至 100 级累计经验 | `117132695` | 旧版武器经验曲线 |

品级权重、能力倍率和价格系数已经与旧版品级口径一致：权重为 `20/50/160/650/3000`，能力倍率为 `1/1.08/1.18/1.32/1.5`，价格系数为 `1/1.5/2.5/4/7`，本轮不重复修改。

## 宏观迁移结果

- 939 件物品的原有相对价差不变，统一把 `参考价` 放大 10 倍：灵植 `220–340`，灵矿 `340–520`，兽宝 `340–2300`，恢复丹 `90–420`，战丹 `1800–32000`，突破丹 `19000–197800`，特殊丹 `1200000–1800000`。
- 160 个敌人全部补齐人物经验，并按等级重算人物经验、本命武器经验和灵石。当前人物经验范围 `102–2280`，武器经验范围 `55–880`，灵石范围 `52–2600`。
- 灵兽修炼成本为修士的 1.25 倍；共享每级攻击为 `0.08`，物种修正映射后实际每级攻击为 `0.06–0.12`。
- 探险改为 30 分钟、每 90 秒一轮、最多 20 轮。闭关仍为 1 小时 6 轮，作为低于主动探险的离线兜底。
- 市场手续费 `5%`，跑商买入税 `1%`，卖出税 `2%`，普通利润率上限 `45%`，已经进入正式经济规则。

## 没有迁移的内容

- 不复制旧版物品、敌人、地图、命令、数据库和业务流程。
- 不用旧版名称或目录覆盖当前编号实体。
- 不把旧版常量写进代码；正式执行值仍由当前 JSON 提供。
- 后续迁移具体内容时，以本报告记录的单位和量级为边界，再按当前机制单独平衡。
