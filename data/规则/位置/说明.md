# 位置规则

`附近.json` 规定动态修士与静态地点的实际查询范围和返回上限。修士查询先按坐标索引完成水平预筛，再结合世界地势推导的海拔差计算三维距离；位置玩法随后根据人物状态过滤、按最多可见数量截断并分页。候选上限用于保证大量人物聚集在同一地点时仍不会无界读取。

本目录不保存玩家位置。玩家 `xy` 只进入数据库 `player_location`；地点、区域、地形和海拔由世界核心根据 `xy` 推导。
