# 敌人阶段宝石池

本目录按初窥、登堂、入微、圆融、镇域、天灾保存六份独立候选池。`规则/角色/敌方修士.json` 的每个阶梯通过 `宝石池` 引用对应文件。

阶段池直接保存 `物品-宝石-*` 源池文件名字符串，不复制宝石实体，也不为文件名增加 `资源池` 字典。业务只展开当前阶段文件明确列出的源池。

随着阶梯提高，池中显式开放的属性家族组合依次增加；阶段差异仍由角色构筑位和实际随机结果共同形成，不在宝石实体上增加等级、权重或评分字段。
