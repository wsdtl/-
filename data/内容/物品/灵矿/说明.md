# 灵矿池说明

灵矿是地脉、岩层和水泽沉积形成的矿物类物品，使用 `22` 前缀六位编号。每个地形对应一个 `灵矿-<地形>` 池，地点和敌人掉落池引用它，运行时只展开编号和权重。

当前灵矿只作为探索产出数据，不绑定炼药、炼器或战斗效果。后续增加用途时，应由独立规则和微服务解释，不把用途字段提前塞进矿石实体。

灵矿池只保存灵矿实体，不混入灵植、兽宝或丹药。池名必须是 `灵矿-<地形>`，地点和敌人掉落由地形自动派生；改名时同步检查地形和炼器归脉引用，编号创建后保持稳定。
