# 全境道路关系说明

本目录保存晓楠修仙界实际存在的道路。文件名就是道路类别，根节点直接使用道路字典列表，不增加同名外层键。

## 当前路网

| 文件 | 数量 | 主要用途 |
| --- | ---: | --- |
| `官道.json` | 25 | 州城、城镇、州际和南境至防线主路 |
| `乡道.json` | 20 | 村庄与附近城镇的日常道路 |
| `山道.json` | 4 | 南境高山聚落的攀行道路 |
| `堤道.json` | 18 | 镜湖州与玄河州的湖泽、河网道路 |
| `军道.json` | 24 | 镇岳防线关隘、军镇和横向调动道路 |
| `荒径.json` | 16 | 防线以北荒原、林海、群山和禁地路径 |
| `栈道.json` | 6 | 高山、断崖和天裂禁地的极险路径 |

共113条道路，覆盖全部80个地点并形成一个连通网络。

## 单条道路

```json
{
  "起点": "青溪村",
  "终点": "青溪镇",
  "途经坐标": [[8, 8], [8, 9], [8, 10], [8, 11], [8, 12], [8, 13], [8, 14]]
}
```

起点和终点引用地点文件名。`途经坐标` 保存从起点到终点的连续 `(x, y)`，每一步只能移动到八个相邻格之一；首格和末格必须分别等于两个地点的水平坐标。道路默认双向，不重复保存反向道路。

道路不保存 `z`、距离、坡度、评分、推荐境界或使用记录。运行时从 `地势.json` 取得每格高度，再按实际行进方向计算路线剖面；发生地势修改时道路无需复制高度，但必须重新校验最优路线与行程叙事。

## 引用关系

```text
晓楠修仙界.json.道路 ------------------> 本目录七个道路文件
道路.起点/终点 ------------------------> 地点文件名
道路.途经坐标 ------------------------> 地势.json 的 (x, y)
定义/行路/行程指标.json --------------> 可计算的路线指标
规则/行路/行程.json ------------------> 全地表坐标连续性、自动选路和路程折算
规则/行路/道路通行.json --------------> 正式道路坐标边的通行倍率
规则/行路/地形通行.json --------------> 非道路坐标边的通行倍率
展示/行程.json -----------------------> 玩家看到的行程叙事
```

## 修改检查

- 新路的端点必须是已登记地点，首尾坐标必须与地点一致。
- 相邻坐标不得越界、跳格或重复，整条路不能形成自环。
- 同一对地点当前只能存在一条直接道路；需要并行路线时必须先增加稳定道路身份规则。
- 删除道路后仍须保证需要开放的地点可从出生地到达。
- `WorldService` 已校验路网，`TravelService` 已执行自动选路和文本生成；玩家位置与行程进度由未来业务维护。
