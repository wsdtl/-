# 敌人阶段气机池

本目录按初窥、登堂、入微、圆融、镇域、天灾保存六份独立候选池。`规则/角色/主体/敌方修士.json` 的每个阶梯通过 `气机池` 引用对应文件。

阶段池直接保存 `气机-*` 源池文件名字符串，不复制气机实体，也不为文件名增加 `资源池` 字典。业务只展开当前阶段文件明确列出的源池。

随着阶梯提高，池中显式开放的属性家族组合依次增加；阶段差异仍由角色构筑位和实际随机结果共同形成。阶段池不另写权重，抽取继续使用气机实体自身的权重；气机不保存等级或评分字段。
