# 道侣气机池

本目录保存264名道侣各自的气机候选池。文件名使用“道侣编号-气机池”，不包含可变的角色名称；道侣实体通过 `气机池` 保存文件名引用。

每份个人池包含24道气机的稳定编号。候选从全部703道气机中选择，并按道侣身份保留不同属性侧重；随机构筑时再从个人池中抽取实际装配数量。

根节点直接使用编号字符串列表，不为编号增加单键字典，不复制气机实体，也不保存名称、评分、权重或方向标签。
